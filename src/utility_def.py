# print (a)

a = [0]
b = sum(k for k in a)
print (b)
## // Standard python Functions used
# list summation
# set substraction
# list sum
# set
# list
# range
# bool
# len
# str
# enumerate
# copy.copy
# random.choice
# random.sample
# random.shuffle



#     cell.visited = True
#     n_cell = cell.next
#     if n_cell == None:
#         pass
#     else:
#         assign_value(matrix_cls,n_cell.emp,n_cell.day,iter1[1:],iter2[1:])
# else:
#     print ("working")
#     # cell.search_space = cell.search_space + cell.p_domain[-1]  #Reset Search space to one step back pruning 3
#     cell.search_space = cell.search_space + cell.p_domain[-1]  #Reset Search space to one step back pruning 2
#     cell.search_space = cell.search_space + cell.p_domain[-1]  #Reset Search space to one step back pruning 1
#     p_cell = cell.prev
#     if p_cell == None:
#         assign_value(matrix_cls,emp,day,iter1,iter2)
